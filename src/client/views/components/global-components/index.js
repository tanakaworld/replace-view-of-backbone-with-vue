import Vue from 'vue';

import GlobalButton from './GlobalButton';
import GlobalForm from './GlobalForm';
import GlobalFormGroup from './GlobalFormGroup';

Vue.use(GlobalButton);
Vue.use(GlobalForm);
Vue.use(GlobalFormGroup);
